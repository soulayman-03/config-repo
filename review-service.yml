spring:
  application:
    name: review-service
  datasource:
    url: jdbc:mysql://localhost:3306/reviewdb
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update

server:
  port: 8083
